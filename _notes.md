## 茶绘 v1.5.1

### 新增

**高斯模糊** —— 半径 0~60px 可调，预览即最终效果。

用 canvas 原生的真高斯；先把原图按边缘像素延展（3×3 平铺到一张带 padding 的大画布）再模糊，
避免四条边被透明稀释成一圈灰雾；最后用「轮廓膨胀」当遮罩，透明处不会变成一坨灰。

菜单里**最后一个「暂未实现」也没了**。

### 改动

**左侧面板整体挪到右边** —— 现在是：画布 | 工具面板 | 聊天侧栏。

聊天 / 成员 / 笔迹侧栏可以收拉：标签栏右侧的 » 收起，收起后留一条窄条 «，点一下拉回来。
收起后画布会自动铺满（不会留空档）。实测收起后画布从 1038px 变成 1320px。

**参考图改成独立浮窗** —— 像 PS 里另开一张图：独立窗口、拖标题栏移动、拖右下角缩放、
调透明度、随时关闭；位置和尺寸记在本地。
仍然是**只有自己看得见**，不会同步给别人。

**菜单两处**
- 尺子菜单去掉了「抖动修正 ±」，改成一句指路（手抖修正在画布上沿的快捷条里），
  点了会把快捷条拉出来并闪一下那个数字
- 「其他」里的「系统 ID」改成显示当前版本（点击打开关于页）

### 下载

| 文件 | 说明 |
| --- | --- |
| `chahui-setup-1.5.1.exe` | 安装版 |
| `chahui-portable-1.5.1.exe` | 免安装，双击即用 |

Windows x64。装之前请先卸载旧版本。

### 测试

```
test-layout 27/27 · test-shell 29/29 · test-personal 13/13 · test-filters 29/29
test-browser 116/116 · test-text 20/20 · test-ruler 23/23 · test-mesh 16/16
test-export 29/29 · test-menu 20/20 · test-selection 27/27 · test-clients 67/67
verify-issues 47/47 · verify-round2 31/31 · verify-round3 23/23
```
